# OneRAG Docker-Free 로컬 Easy Start 환경 설정
#
# 사용법:
#   cp easy_start/.env.local .env
#   # .env에서 API 키 하나만 설정 (Gemini 또는 OpenRouter)
#   make easy-start

# ============================================================
# 필수 설정 - LLM API 키 (하나만 선택)
# ============================================================

# Option 1: Google Gemini (추천 - 무료 티어 제공)
# 발급: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Option 2: OpenRouter (다양한 모델 지원, 무료 모델 포함)
# 발급: https://openrouter.ai/keys
# OPENROUTER_API_KEY=your-openrouter-api-key-here

# ============================================================
# 자동 설정 (수정 불필요)
# ============================================================

ENVIRONMENT=development

# 벡터 DB: ChromaDB (Docker 불필요!)
VECTOR_DB_PROVIDER=chroma
CHROMA_PERSIST_DIR=./easy_start/.chroma_data

# 임베딩: 로컬 모델 (API 키 불필요)
EMBEDDINGS_PROVIDER=local

# LLM 설정
LLM_PROVIDER=google
LLM_MODEL=gemini-3-flash-preview

# 서버 설정
HOST=0.0.0.0
PORT=8000
DEBUG=true

# 인증 (개발용)
FASTAPI_AUTH_KEY=quickstart-dev-key-change-in-production
