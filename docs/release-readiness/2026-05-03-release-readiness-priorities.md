# OneRAG Release Readiness Priorities

Date: 2026-05-03
Baseline: `origin/main` after PR #2, #3, #4 merges (`ad08f19`)

Status update: 2026-05-13. This document is a historical prioritization
snapshot. Current operational status is tracked in `docs/STATUS.md`.

## Evidence Used

- `npm ci` succeeded in a clean temp clone.
- `npm run build` failed because `./icons/BrandLogo` is imported but not present in tracked source.
- `docker-compose.yml` references `frontend/Dockerfile.local`, but only `frontend/Dockerfile` is tracked.
- `.github/workflows/ci.yml` runs Python checks only.
- Two independent auditor subagents agreed on the main release blockers.
- Official Codex docs show subagents, skills, GitHub Action workflows, guardrails, and evals as first-class surfaces for this kind of workflow.

## Current Resolution Summary (2026-05-13)

- Frontend build, lint, and test are covered by CI.
- Backend coverage artifacts are generated by CI.
- Runtime smoke is covered by `make test-operational-smoke` and the CI `Runtime Smoke` job.
- Docker API healthchecks use `/ready`.
- Quickstart env template exists and sample-data loaders are non-destructive by default.
- OSS governance files and PR template are present.
- Remaining items are follow-up quality improvements, not current P0 release blockers.

## Top 10 Priorities

| Rank | Work | Effect | Difficulty | Baseline reason | Current status | Dependencies | Parallel Group |
|---:|---|---:|---:|---|---|---|---|
| 1 | Restore frontend production build by tracking/fixing `BrandLogo` imports and ignored icon assets | 5 | 1 | `npm run build` failed on `PageHeader.tsx`/`Sidebar.tsx` imports | ✅ Resolved; frontend CI passes | None | A |
| 2 | Fix Docker fullstack compose frontend build path | 5 | 1 | `docker-compose.yml` pointed to missing `frontend/Dockerfile.local` | ✅ Resolved; compose config smoke passes | None | A |
| 3 | Restore quickstart `.env` template and command consistency | 5 | 1 | README/Makefile referenced `quickstart/.env.quickstart`; baseline auditors found it missing | ✅ Resolved; template tracked and docs updated | None | A |
| 4 | Add frontend CI gate: `npm ci`, `npm run build`, `npm run lint`, `npm run test:run` | 5 | 2 | Frontend failures reached `main` because CI did not cover frontend | ✅ Resolved; CI runs warning-gated build/test plus lint | 1 | B |
| 5 | Make backend coverage artifact real and verify claimed test counts | 4 | 2 | CI uploaded `htmlcov/` without generating coverage | ✅ Resolved; CI generates `htmlcov/` and `coverage.xml` | None | B |
| 6 | Add dependency/security gate and automation | 4 | 2 | `npm ci` reported vulnerabilities; no strict audit gate existed | ⏳ Follow-up; dependency triage policy exists, strict gate still optional | 4 preferred | C |
| 7 | Add accessibility regression checks and fix known semantic issues | 4 | 2 | Auditor saw Dialog title/description and invalid HTML warnings | ⏳ Follow-up quality work | 4 preferred | E |
| 8 | Add open-source governance files and templates | 3 | 1 | Governance files/templates were absent | ✅ Resolved; governance docs and PR template present | None | D |
| 9 | Fix packaging/runtime data inclusion | 4 | 2 | Package data likely omitted YAML/JSON/easy-start/quickstart assets | ✅ Resolved for release gates; production image and runtime smoke covered | 3 preferred | G |
| 10 | Repair docs drift: brand, version, setup, missing docs links | 3 | 2 | Docs mentioned older names, versions, and missing files | ✅ Updated 2026-05-13; continue routine docs drift review | 3, 5 preferred | H |

## Parallelization Plan

Wave 1, release boot:
- Group A1: BrandLogo/build blocker.
- Group A2: Docker compose path.
- Group A3: quickstart env template.
- These can run in parallel because write sets are mostly disjoint.

Wave 2, trust gates:
- Group B1: frontend CI.
- Group B2: backend coverage/test-count verification.
- Group C1: dependency/security gate design.
- B1 depends on BrandLogo being fixed if the branch must pass immediately.

Wave 3, quality surface:
- Group E1: accessibility warnings and tests.
- Group D1: open-source governance files.
- Group G1: package data and Docker image improvements.

Wave 4, product depth:
- Group H1: docs drift cleanup.
- Group F1: RAG trace backend payloads and RAGAS/eval fixtures.

## Sequential Gates

1. Fix P0 release boot tasks.
2. Run frontend build/lint/test locally.
3. Add CI gates.
4. Confirm CI passes on PR.
5. Add governance/security/docs after build gates are green.
6. Move to RAG quality and deeper observability after release trust is restored.

## Recommended First PRs

1. `fix/release-boot-assets-compose-quickstart`
2. `ci/frontend-and-coverage-gates`
3. `security/dependency-and-demo-access-hardening`
4. `docs/oss-governance-and-drift`
5. `ux/accessibility-regression-suite`
